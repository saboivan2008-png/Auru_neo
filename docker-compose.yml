version: '3.8'

services:
  aether-agent:
    build: .
    container_name: aether_ai_platform
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
